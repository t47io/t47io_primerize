import * as a from '../constants/actionTypes.js';


export const initialState = {
};

const reducer = (state = initialState, { type, payload }) => {
  switch (type) {
    case 'xx': {
      return state;
    }
    default: {
      return state;
    }
  }
};


export default reducer;
